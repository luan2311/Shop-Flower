{
  "name": "shopflower-tests-ui",
  "version": "1.0.0",
  "description": "E2E Cross-Browser UI Automation Tests for ShopFlower",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.1"
  }
}
